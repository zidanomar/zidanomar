---
interface Props {
    company: string;
}
const { company } = Astro.props;
---
<section id="home" class="section section--landing">
    <div class="landing-card">

        <div class="hero-top reveal-1">
            <div class="avatar">
                <img src="/memoji.png" alt="Zidan's memoji" />
            </div>
            <h1 class="greeting"><span class="typewriter">Hi, I'm Zidan.</span><span class="greeting-cursor" aria-hidden="true">_</span></h1>
        </div>

        <p class="bio reveal-3">
            <span class="bio-typed"></span><span class="bio-cursor" aria-hidden="true">_</span>
        </p>

        <a href="#contact" class="say-hello reveal-4">Say hello</a>

        <script define:vars={{ company }}>
            const BIO_PREFIX = 'Software Engineer with 3+ years of experience building reliable backend systems and cloud infrastructure. Hands-on across the fullstack from protocol level IoT data ingestion and event-driven messaging to container deployments on Kubernetes and CI/CD automation. Currently at ';
            const BIO_LINK = `${company} ↗`;
            const FULL_TEXT = BIO_PREFIX + BIO_LINK;
            const GREETING_DONE = 1200;
            const CHAR_SPEED = 15;

            setTimeout(() => {
                const typed = document.querySelector('.bio-typed');
                const greetingCursor = document.querySelector('.greeting-cursor');
                const button = document.querySelector('.say-hello');
                if (!typed) return;

                if (greetingCursor) greetingCursor.classList.add('hidden');

                let i = 0;
                const interval = setInterval(() => {
                    typed.textContent += FULL_TEXT[i];
                    i++;
                    if (i >= FULL_TEXT.length) {
                        clearInterval(interval);
                        // Wrap company name in a link
                        typed.innerHTML = BIO_PREFIX +
                            `<a href="#experience" class="accent-link">${BIO_LINK}</a>`;
                        if (button) button.classList.add('show');
                    }
                }, CHAR_SPEED);
            }, GREETING_DONE);
        </script>
    </div>
</section>

<style>
.section--landing {
    align-items: center;
}

.landing-card {
    max-width: 720px;
    width: 100%;
}

.hero-top {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.75rem;
}

.avatar {
    position: relative;
    width: 4.5rem;
    height: 4.5rem;
    flex-shrink: 0;
    border-radius: 50%;
    padding: 0.5rem;
    transform: translate(2px, -1px);
}

.avatar::before {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: 50%;
    background: conic-gradient(from 0deg, #f09433, #e6683c, #dc2743, #cc2366, #bc1888, #f09433);
    animation: story-spin 4s linear infinite;
    z-index: 0;
}

.avatar::after {
    content: '';
    position: absolute;
    inset: 2px;
    border-radius: 50%;
    background: var(--bg-primary);
    z-index: 1;
}

.avatar img {
    position: relative;
    z-index: 2;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
    display: block;
    border-radius: 50%;
    transform: translate(1px, -1px);
}

@keyframes story-spin {
    to { transform: rotate(360deg); }
}

.greeting {
    font-size: clamp(1.5rem, 4vw, 2rem);
    font-weight: 600;
    color: var(--text-primary);
    letter-spacing: -0.02em;
    line-height: 1;
    margin: 0;
    padding: 0;
}

.typewriter {
    display: inline-block;
    overflow: hidden;
    white-space: nowrap;
    width: 0;
    animation: typing 0.8s steps(14) 0.4s forwards;
}

@keyframes typing {
    to { width: 14ch; }
}

.greeting-cursor {
    color: var(--accent-color);
    animation: cursor-blink 0.6s step-end infinite;
    font-weight: 300;
}

.greeting-cursor.hidden {
    display: none;
}

@keyframes cursor-blink {
    50% { opacity: 0; }
}

.bio {
    font-size: 1rem;
    color: var(--text-secondary);
    line-height: 1.85;
    margin-bottom: 2rem;
    min-height: 3.5em;
}

.bio-cursor {
    color: var(--accent-color);
    animation: bio-blink 0.6s step-end infinite;
    font-weight: 300;
}

@keyframes bio-blink {
    50% { opacity: 0; }
}

.accent-link {
    color: var(--text-accent);
    text-decoration: none;
    transition: opacity 0.15s ease;
    border-bottom: 1px solid color-mix(in srgb, var(--text-accent) 30%, transparent);
}

.accent-link:hover {
    opacity: 0.8;
    text-decoration: none;
    color: var(--text-accent);
    border-bottom-color: var(--text-accent);
}


.say-hello {
    display: inline-block;
    border: 1px solid var(--border-color);
    color: var(--text-secondary);
    font-family: var(--font-mono);
    font-size: 0.875rem;
    padding: 8px 24px;
    border-radius: 3px;
    text-decoration: none;
    transition: border-color 0.2s ease, color 0.2s ease, background-color 0.2s ease, transform 0.15s ease;
    letter-spacing: 0.04em;
    position: relative;
}

.say-hello:hover {
    border-color: var(--accent-color);
    color: var(--text-accent);
    background-color: color-mix(in srgb, var(--accent-color) 8%, transparent);
    text-decoration: none;
}

.say-hello:active {
    transform: scale(0.97);
}

.reveal-1, .reveal-2, .reveal-3, .reveal-4 {
    opacity: 0;
}

.reveal-1 { animation: fadeUp 0.55s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards 0.05s; }
.reveal-2 { animation: fadeUp 0.55s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards 0.2s; }
.reveal-3 { animation: fadeUp 0.55s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards 1.1s; }
.reveal-4 {
    transition: opacity 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), transform 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    transform: translateY(8px);
}
.reveal-4.show {
    opacity: 1;
    transform: translateY(0);
}

@keyframes fadeUp {
    from { opacity: 0; transform: translateY(16px); }
    to   { opacity: 1; transform: translateY(0); }
}
</style>
