---
interface Education {
    year: string;
    school: string;
    location: string;
    major: string;
}

interface Props {
    educations: Education[];
}

const { educations } = Astro.props;
---
<section id="education" class="section section--education">
    <div class="section-inner">
        <h2 class="section-heading tl-reveal">education</h2>

        <div class="edu-list">
            {educations.map((edu, i) => (
                <div class="edu-item tl-reveal" style={`transition-delay: ${i * 80}ms`}>
                    <div class="edu-header">
                        <span class="edu-school">{edu.school}</span>
                        <span class="edu-year">{edu.year}</span>
                    </div>
                    <div class="edu-meta">
                        <span class="edu-major">{edu.major}</span>
                        {edu.location && <span class="edu-location">{edu.location}</span>}
                    </div>
                </div>
            ))}
        </div>
    </div>
</section>

<style>
.section--education {
    border-top: 1px solid var(--border-color);
}

.edu-list {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
}

.edu-item {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding-left: 0.875rem;
    border-left: 2px solid var(--border-color);
    transition:
        border-color 0.2s ease,
        opacity 0.55s cubic-bezier(0.25, 0.46, 0.45, 0.94),
        transform 0.55s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.edu-item:hover {
    border-left-color: var(--accent-color);
}

.edu-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
}

.edu-school {
    font-size: 1rem;
    color: var(--text-primary);
    font-weight: 600;
}

.edu-year {
    font-size: 0.8rem;
    color: var(--text-accent);
    letter-spacing: 0.04em;
    flex-shrink: 0;
}

.edu-meta {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
}

.edu-major {
    font-size: 0.8rem;
    color: var(--text-accent);
    opacity: 0.8;
    letter-spacing: 0.03em;
}

.edu-location {
    font-size: 0.8rem;
    color: var(--comment-color);
    letter-spacing: 0.02em;
    flex-shrink: 0;
}

@media (max-width: 768px) {
    .edu-meta {
        flex-direction: column-reverse;
        align-items: flex-start;
        gap: 0.25rem;
    }

    .edu-location {
        margin-top: 0.25rem;
    }
}
</style>
